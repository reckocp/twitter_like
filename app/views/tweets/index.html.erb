<h1> All Tweets </h1>
<a href="/users">GO TO ALL USERS</a>


  <div>
    <ul>
      <% @tweets.each do |tweet| %>

        <li>
          <p><%= tweet.message %> </p>
        <a href="/tweets/<%= tweet.id %>" > VIEW THIS TWEET</a></li>
      <% end %>
    </ul>
  </div>
<%= will_paginate @tweets %>
